context('Tailwind', () => {

  describe('Using combined Tailwind config', () => {

    it('uses `bg-testcolor` in a component', () => {

    })

    it('creates `testcolor` in site TW config', () => {

    })

    it('reflects color change in component', () => {

    })

    it('overwrites `testcolor` in page TW config', () => {

    })

    it('reflects color change in component', () => {

    })

    it('reflects color change in component preview', () => {

    })

    it('reflects color change after page refresh', () => {

    })

  })

})
